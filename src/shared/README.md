# Shared application code

Only code used by multiple domain modules belongs here. Keep business-specific
rules inside their owning module and infrastructure-specific code under
`src/infrastructure`.
